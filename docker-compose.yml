version: "3.9"

services:
  webapp:
    build:
      context: ./webapp
      args:
        NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    network_mode: bridge
    environment:
      - NGROK_URL=${NGROK_URL}
      - ROS_DOMAIN_ID=${ROS_DOMAIN_ID}
      - BARCODE_ROS2_TOPIC_NAME=${BARCODE_ROS2_TOPIC_NAME}
